import YTIframeVideoPlayer from './presentational.jsx';
import { connect } from 'react-redux';

const mapStateToProps = (state, props) => {
  return {

  };
};

const mapDispatchToProps = (dispatch, props) => {
  return {

  };
};

module.exports = connect(
  mapStateToProps,
  mapDispatchToProps
)(YTIframeVideoPlayer);
