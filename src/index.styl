@import './vars.styl'

*
  position relative
  box-sizing border-box

body
  display flex
  flex-flow row nowrap
  align-items center
  justify-content center
  padding 0
  margin 0
  width 100vw
  height 100vh
  background $masking_color
  overflow hidden

foovar('src/StyleVariables.js', {
  include: '^\$'
})
