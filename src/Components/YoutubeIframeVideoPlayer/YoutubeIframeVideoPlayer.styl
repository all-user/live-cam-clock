@import '../../vars.styl'

.wrapper
  absolute top -100% bottom -100% left -100% right -100%
  margin auto
  z-index 0
  overflow hidden
  iframe
    display block
    absolute top -100% bottom -100% left -100% right -100%
    margin auto
    z-index 0
